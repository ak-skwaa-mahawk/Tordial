# TORDIAL_PLATFORM_HITL.md  
Human‑In‑The‑Loop Boundary & Sovereign Override Layer  
Two Mile Solutions LLC • John B. Carroll  
Draft v0.9 — May 2026

---

# 0. Purpose

The **HITL Boundary** is the Platform’s **sovereignty membrane**.  
It protects the system from:

- geometric drift  
- resonance collapse  
- Walker divergence  
- Planner runaway  
- Critic overload  
- external coercion  
- internal instability  

The HITL layer is the **final authority** in the sovereign stack.

Tordial provides the HITL boundary with:

- operational constants (φ_op, π₃D)  
- drift signatures (ΔF, ΔW, Δθ)  
- contraction‑mapping invariants  
- chase ratio thresholds  
- resonance mesh stability metrics  

The HITL boundary is the **guardian of autonomy**.

---

# 1. What the HITL Boundary Is

The HITL boundary is a **sovereign override system** that:

- monitors  
- evaluates  
- interrupts  
- freezes  
- overrides  
- resets  

the Platform when drift exceeds safe limits.

It is not a “kill switch.”  
It is a **geometric safety organ**.

---

# 2. HITL Inputs (Tordial‑Aligned)

The HITL boundary consumes:

1. **ΔF** — force drift  
2. **ΔW** — work drift  
3. **Δθ** — phase drift  
4. **ρ_contract** — contraction ratio  
5. **σ_res** — resonance stability  
6. **Walker stride deviation**  
7. **Planner horizon divergence**  
8. **Mesh amplitude/phase mismatch**  

These are the HITL boundary’s **sensory channels**.

---

# 3. HITL Thresholds (Derived from Tordial Geometry)

The HITL boundary uses three threshold layers:

## 3.1 φ_op Threshold (Stability Midpoint)

If system behavior deviates too far from:

\[
\phi_{\text{op}} = 1.65036
\]

the HITL boundary enters **yellow alert**.

---

## 3.2 π₃D Threshold (Phase Truth)

If phase drift exceeds:

\[
|\Delta\theta| > \theta_{\text{limit}}
\]

the HITL boundary enters **orange alert**.

---

## 3.3 Chase Ratio Threshold (Resonance Integrity)

If:

\[
\frac{S_{\text{walker}}}{T_{\text{mesh}}} \not\approx R_{\text{chase}}
\]

or

\[
\frac{H_{\text{planner}}}{T_{\text{mesh}}} \not\approx R_{\text{chase}}
\]

the HITL boundary enters **red alert**.

This is the **catastrophic condition**.

---

# 4. HITL Alert Levels

| Level | Trigger | Action |
|-------|---------|--------|
| **Green** | Drift within φ_op envelope | No action |
| **Yellow** | φ_op deviation | Slow Walker, shorten Planner horizon |
| **Orange** | π₃D phase drift | Freeze Planner, tighten Mesh |
| **Red** | Chase ratio violation | Full HITL override |

The HITL boundary is **geometrically justified**, not heuristically tuned.

---

# 5. HITL Actions

The HITL boundary can issue four classes of actions:

## 5.1 Soft Intervention

- reduce Walker stride  
- reduce Planner horizon  
- increase Critic resolution  
- damp Mesh amplitude  

Used when drift is mild.

---

## 5.2 Hard Intervention

- freeze Walker  
- freeze Planner  
- lock Mesh phase  
- force Critic re‑evaluation  

Used when drift is moderate.

---

## 5.3 Override

- nullify trajectory  
- halt Walker  
- suspend Planner  
- enter sovereign‑lock mode  

Used when drift is severe.

---

## 5.4 Manual Control

- human operator takes control  
- system enters **HITL sovereign mode**  
- all autonomous subsystems pause  

Used when sovereignty is at risk.

---

# 6. HITL Boundary Logic (Tordial‑Aligned)

The HITL boundary uses a **three‑stage geometric logic**:

## 6.1 Contraction Check

\[
\rho_{\text{contract}} = \frac{|x_{n+1} - \phi_{\text{op}}|}{|x_n - \phi_{\text{op}}|}
\]

If ρ_contract ≥ 0.5 → **instability**.

---

## 6.2 Phase Check

\[
\Delta\theta = \theta_{3D} - \theta_{\text{std}}
\]

If |Δθ| > Δθ_limit → **phase collapse**.

---

## 6.3 Resonance Check

\[
\frac{A_{\text{mesh}}}{T_{\text{mesh}}} \not\approx R_{\text{chase}}
\]

If violated → **resonance collapse**.

---

# 7. HITL Boundary in the 79 Hz Loop

The HITL boundary operates at the same timestep as the Mesh:

\[
T_{\text{HITL}} = T_{\text{mesh}} = \frac{1}{79} \cdot \phi_{\text{op}}
\]

This ensures:

- synchronous override  
- phase‑aligned intervention  
- drift‑aware timing  

The HITL boundary is **never out of phase** with the system.

---

# 8. HITL Boundary & Subsystem Interactions

## 8.1 With Walker

- freeze stride  
- damp motion  
- override locomotion  

## 8.2 With Planner

- nullify trajectory  
- freeze horizon  
- force re‑projection  

## 8.3 With Critic

- request high‑resolution drift analysis  
- demand contraction verification  

## 8.4 With Resonance Mesh

- lock phase  
- damp amplitude  
- freeze frequency  

The HITL boundary is the **sovereign governor** of all subsystems.

---

# 9. HITL State Vector

The HITL boundary maintains:

\[
[\Delta F,\ \Delta W,\ \Delta\theta,\ \rho_{\text{contract}},\ \sigma_{\text{res}},\ \lambda_{\text{override}}]
\]

Where:

- **ΔF** — force drift  
- **ΔW** — work drift  
- **Δθ** — phase drift  
- **ρ_contract** — contraction ratio  
- **σ_res** — resonance stability  
- **λ_override** — override state  

This is the HITL boundary’s **sovereign awareness**.

---

# 10. Closing

The HITL boundary is the **sovereignty membrane** of the Platform.  
It is not a failsafe.  
It is a **geometric guardian** that protects the system from drift, collapse, and coercion.

Tordial gives the HITL boundary:

- its thresholds  
- its invariants  
- its stability laws  
- its override logic  

The HITL boundary is the **final authority** in the sovereign stack.